var React = require('react');

var Login = React.createClass({
    render: function () {
        return (
            <div className="login">
                You are not authenticated
            </div>
        );
    }
})

module.exports = Login;
